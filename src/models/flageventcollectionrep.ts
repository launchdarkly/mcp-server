/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  FlagEventRep,
  FlagEventRep$inboundSchema,
  FlagEventRep$Outbound,
  FlagEventRep$outboundSchema,
} from "./flageventrep.js";
import {
  Link,
  Link$inboundSchema,
  Link$Outbound,
  Link$outboundSchema,
} from "./link.js";

export type FlagEventCollectionRep = {
  /**
   * The total number of flag events
   */
  totalCount: number;
  /**
   * A list of flag events
   */
  items: Array<FlagEventRep>;
  /**
   * The location and content type of related resources
   */
  links?: { [k: string]: Link } | undefined;
};

/** @internal */
export const FlagEventCollectionRep$inboundSchema: z.ZodType<
  FlagEventCollectionRep,
  z.ZodTypeDef,
  unknown
> = z.object({
  totalCount: z.number().int(),
  items: z.array(FlagEventRep$inboundSchema),
  _links: z.record(Link$inboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

/** @internal */
export type FlagEventCollectionRep$Outbound = {
  totalCount: number;
  items: Array<FlagEventRep$Outbound>;
  _links?: { [k: string]: Link$Outbound } | undefined;
};

/** @internal */
export const FlagEventCollectionRep$outboundSchema: z.ZodType<
  FlagEventCollectionRep$Outbound,
  z.ZodTypeDef,
  FlagEventCollectionRep
> = z.object({
  totalCount: z.number().int(),
  items: z.array(FlagEventRep$outboundSchema),
  links: z.record(Link$outboundSchema).optional(),
}).transform((v) => {
  return remap$(v, {
    links: "_links",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace FlagEventCollectionRep$ {
  /** @deprecated use `FlagEventCollectionRep$inboundSchema` instead. */
  export const inboundSchema = FlagEventCollectionRep$inboundSchema;
  /** @deprecated use `FlagEventCollectionRep$outboundSchema` instead. */
  export const outboundSchema = FlagEventCollectionRep$outboundSchema;
  /** @deprecated use `FlagEventCollectionRep$Outbound` instead. */
  export type Outbound = FlagEventCollectionRep$Outbound;
}

export function flagEventCollectionRepToJSON(
  flagEventCollectionRep: FlagEventCollectionRep,
): string {
  return JSON.stringify(
    FlagEventCollectionRep$outboundSchema.parse(flagEventCollectionRep),
  );
}

export function flagEventCollectionRepFromJSON(
  jsonString: string,
): SafeParseResult<FlagEventCollectionRep, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => FlagEventCollectionRep$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'FlagEventCollectionRep' from JSON`,
  );
}
