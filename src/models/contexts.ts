/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import {
  ContextRecord,
  ContextRecord$inboundSchema,
  ContextRecord$Outbound,
  ContextRecord$outboundSchema,
} from "./contextrecord.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  Link,
  Link$inboundSchema,
  Link$Outbound,
  Link$outboundSchema,
} from "./link.js";

export type Contexts = {
  /**
   * The location and content type of related resources
   */
  links?: { [k: string]: Link } | undefined;
  /**
   * The number of contexts
   */
  totalCount?: number | undefined;
  /**
   * The environment ID where the context was evaluated
   */
  environmentId: string;
  /**
   * An obfuscated string that references the last context instance on the previous page of results. You can use this for pagination, however, we recommend using the <code>next</code> link instead.
   */
  continuationToken?: string | undefined;
  /**
   * A collection of contexts. Can include multiple versions of contexts that have the same <code>kind</code> and <code>key</code>, but different <code>applicationId</code>s.
   */
  items: Array<ContextRecord>;
};

/** @internal */
export const Contexts$inboundSchema: z.ZodType<
  Contexts,
  z.ZodTypeDef,
  unknown
> = z.object({
  _links: z.record(Link$inboundSchema).optional(),
  totalCount: z.number().int().optional(),
  _environmentId: z.string(),
  continuationToken: z.string().optional(),
  items: z.array(ContextRecord$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
    "_environmentId": "environmentId",
  });
});

/** @internal */
export type Contexts$Outbound = {
  _links?: { [k: string]: Link$Outbound } | undefined;
  totalCount?: number | undefined;
  _environmentId: string;
  continuationToken?: string | undefined;
  items: Array<ContextRecord$Outbound>;
};

/** @internal */
export const Contexts$outboundSchema: z.ZodType<
  Contexts$Outbound,
  z.ZodTypeDef,
  Contexts
> = z.object({
  links: z.record(Link$outboundSchema).optional(),
  totalCount: z.number().int().optional(),
  environmentId: z.string(),
  continuationToken: z.string().optional(),
  items: z.array(ContextRecord$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    links: "_links",
    environmentId: "_environmentId",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace Contexts$ {
  /** @deprecated use `Contexts$inboundSchema` instead. */
  export const inboundSchema = Contexts$inboundSchema;
  /** @deprecated use `Contexts$outboundSchema` instead. */
  export const outboundSchema = Contexts$outboundSchema;
  /** @deprecated use `Contexts$Outbound` instead. */
  export type Outbound = Contexts$Outbound;
}

export function contextsToJSON(contexts: Contexts): string {
  return JSON.stringify(Contexts$outboundSchema.parse(contexts));
}

export function contextsFromJSON(
  jsonString: string,
): SafeParseResult<Contexts, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Contexts$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Contexts' from JSON`,
  );
}
