/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  LayerRep,
  LayerRep$inboundSchema,
  LayerRep$Outbound,
  LayerRep$outboundSchema,
} from "./layerrep.js";
import {
  Link,
  Link$inboundSchema,
  Link$Outbound,
  Link$outboundSchema,
} from "./link.js";

export type LayerCollectionRep = {
  /**
   * The layers in the project
   */
  items: Array<LayerRep>;
  /**
   * The total number of layers in the project
   */
  totalCount: number;
  /**
   * The location and content type of related resources
   */
  links: { [k: string]: Link };
};

/** @internal */
export const LayerCollectionRep$inboundSchema: z.ZodType<
  LayerCollectionRep,
  z.ZodTypeDef,
  unknown
> = z.object({
  items: z.array(LayerRep$inboundSchema),
  totalCount: z.number().int(),
  _links: z.record(Link$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "_links": "links",
  });
});

/** @internal */
export type LayerCollectionRep$Outbound = {
  items: Array<LayerRep$Outbound>;
  totalCount: number;
  _links: { [k: string]: Link$Outbound };
};

/** @internal */
export const LayerCollectionRep$outboundSchema: z.ZodType<
  LayerCollectionRep$Outbound,
  z.ZodTypeDef,
  LayerCollectionRep
> = z.object({
  items: z.array(LayerRep$outboundSchema),
  totalCount: z.number().int(),
  links: z.record(Link$outboundSchema),
}).transform((v) => {
  return remap$(v, {
    links: "_links",
  });
});

/**
 * @internal
 * @deprecated This namespace will be removed in future versions. Use schemas and types that are exported directly from this module.
 */
export namespace LayerCollectionRep$ {
  /** @deprecated use `LayerCollectionRep$inboundSchema` instead. */
  export const inboundSchema = LayerCollectionRep$inboundSchema;
  /** @deprecated use `LayerCollectionRep$outboundSchema` instead. */
  export const outboundSchema = LayerCollectionRep$outboundSchema;
  /** @deprecated use `LayerCollectionRep$Outbound` instead. */
  export type Outbound = LayerCollectionRep$Outbound;
}

export function layerCollectionRepToJSON(
  layerCollectionRep: LayerCollectionRep,
): string {
  return JSON.stringify(
    LayerCollectionRep$outboundSchema.parse(layerCollectionRep),
  );
}

export function layerCollectionRepFromJSON(
  jsonString: string,
): SafeParseResult<LayerCollectionRep, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LayerCollectionRep$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LayerCollectionRep' from JSON`,
  );
}
