/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsGetBigSegmentImport } from "../../funcs/segmentsGetBigSegmentImport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBigSegmentImportRequest$inboundSchema,
};

export const tool$segmentsGetBigSegmentImport: ToolDefinition<typeof args> = {
  name: "segments-get-big-segment-import",
  description: `Get big segment import

Returns information about a big segment import process. This is the import of a list-based segment that can include more than 15,000 entries.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsGetBigSegmentImport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
