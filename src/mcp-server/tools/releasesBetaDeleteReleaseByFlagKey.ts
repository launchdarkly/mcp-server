/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { releasesBetaDeleteReleaseByFlagKey } from "../../funcs/releasesBetaDeleteReleaseByFlagKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteReleaseByFlagKeyRequest$inboundSchema,
};

export const tool$releasesBetaDeleteReleaseByFlagKey: ToolDefinition<
  typeof args
> = {
  name: "releases-(beta)-delete-release-by-flag-key",
  description: `Delete a release for flag

Deletes a release from a flag`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await releasesBetaDeleteReleaseByFlagKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
