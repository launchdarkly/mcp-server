/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowsGetWorkflows } from "../../funcs/workflowsGetWorkflows.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetWorkflowsRequest$inboundSchema,
};

export const tool$workflowsGetWorkflows: ToolDefinition<typeof args> = {
  name: "workflows-get-workflows",
  description: `Get workflows

Display workflows associated with a feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsGetWorkflows(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
