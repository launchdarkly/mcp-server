/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksPostWebhook } from "../../funcs/webhooksPostWebhook.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.WebhookPost$inboundSchema,
};

export const tool$webhooksPostWebhook: ToolDefinition<typeof args> = {
  name: "webhooks-post-webhook",
  description: `Creates a webhook

Create a new webhook.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksPostWebhook(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
