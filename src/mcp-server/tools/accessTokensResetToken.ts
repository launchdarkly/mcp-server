/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accessTokensResetToken } from "../../funcs/accessTokensResetToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ResetTokenRequest$inboundSchema,
};

export const tool$accessTokensResetToken: ToolDefinition<typeof args> = {
  name: "access-tokens-reset-token",
  description: `Reset access token

Reset an access token's secret key with an optional expiry time for the old key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accessTokensResetToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
