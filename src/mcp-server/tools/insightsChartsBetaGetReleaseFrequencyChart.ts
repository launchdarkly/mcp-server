/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsChartsBetaGetReleaseFrequencyChart } from "../../funcs/insightsChartsBetaGetReleaseFrequencyChart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetReleaseFrequencyChartRequest$inboundSchema,
};

export const tool$insightsChartsBetaGetReleaseFrequencyChart: ToolDefinition<
  typeof args
> = {
  name: "insights-charts-(beta)-get-release-frequency-chart",
  description: `Get release frequency chart data

Get release frequency chart data. Engineering insights displays release frequency data in the [release frequency metric view](https://launchdarkly.com/docs/home/observability/releases).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsChartsBetaGetReleaseFrequencyChart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
