/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationAuditLogSubscriptionsGetSubscriptions } from "../../funcs/integrationAuditLogSubscriptionsGetSubscriptions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSubscriptionsRequest$inboundSchema,
};

export const tool$integrationAuditLogSubscriptionsGetSubscriptions:
  ToolDefinition<typeof args> = {
    name: "integration-audit-log-subscriptions-get-subscriptions",
    description: `Get audit log subscriptions by integration

Get all audit log subscriptions associated with a given integration.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await integrationAuditLogSubscriptionsGetSubscriptions(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
