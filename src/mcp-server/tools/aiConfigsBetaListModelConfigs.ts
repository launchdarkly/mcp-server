/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaListModelConfigs } from "../../funcs/aiConfigsBetaListModelConfigs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListModelConfigsRequest$inboundSchema,
};

export const tool$aiConfigsBetaListModelConfigs: ToolDefinition<typeof args> = {
  name: "AI-configs-(beta)-list-model-configs",
  description: `List AI model configs

Get all AI model configs for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaListModelConfigs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
