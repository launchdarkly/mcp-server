/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagLinksBetaUpdateFlagLink } from "../../funcs/flagLinksBetaUpdateFlagLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateFlagLinkRequest$inboundSchema,
};

export const tool$flagLinksBetaUpdateFlagLink: ToolDefinition<typeof args> = {
  name: "flag-links-(beta)-update-flag-link",
  description: `Update flag link

Update a flag link. Updating a flag link uses a [JSON patch](https://datatracker.ietf.org/doc/html/rfc6902) representation of the desired changes. To learn more, read [Updates](https://launchdarkly.com/docs/api#updates).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagLinksBetaUpdateFlagLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
