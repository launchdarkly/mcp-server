/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { relayProxyConfigurationsGetRelayProxyConfig } from "../../funcs/relayProxyConfigurationsGetRelayProxyConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRelayProxyConfigRequest$inboundSchema,
};

export const tool$relayProxyConfigurationsGetRelayProxyConfig: ToolDefinition<
  typeof args
> = {
  name: "relay-proxy-configurations-get-relay-proxy-config",
  description: `Get Relay Proxy config

Get a single Relay Proxy auto config by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await relayProxyConfigurationsGetRelayProxyConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
