/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { persistentStoreIntegrationsBetaDeleteBigSegmentStoreIntegration } from "../../funcs/persistentStoreIntegrationsBetaDeleteBigSegmentStoreIntegration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteBigSegmentStoreIntegrationRequest$inboundSchema,
};

export const tool$persistentStoreIntegrationsBetaDeleteBigSegmentStoreIntegration:
  ToolDefinition<typeof args> = {
    name:
      "persistent-store-integrations-(beta)-delete-big-segment-store-integration",
    description: `Delete big segment store integration

Delete a persistent store integration. Each integration uses either Redis or DynamoDB.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await persistentStoreIntegrationsBetaDeleteBigSegmentStoreIntegration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
