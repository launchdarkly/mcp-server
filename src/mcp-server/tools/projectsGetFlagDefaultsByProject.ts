/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsGetFlagDefaultsByProject } from "../../funcs/projectsGetFlagDefaultsByProject.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFlagDefaultsByProjectRequest$inboundSchema,
};

export const tool$projectsGetFlagDefaultsByProject: ToolDefinition<
  typeof args
> = {
  name: "projects-get-flag-defaults-by-project",
  description: `Get flag defaults for project

Get the flag defaults for a specific project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsGetFlagDefaultsByProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
