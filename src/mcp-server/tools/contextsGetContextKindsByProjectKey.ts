/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextsGetContextKindsByProjectKey } from "../../funcs/contextsGetContextKindsByProjectKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetContextKindsByProjectKeyRequest$inboundSchema,
};

export const tool$contextsGetContextKindsByProjectKey: ToolDefinition<
  typeof args
> = {
  name: "contexts-get-context-kinds-by-project-key",
  description: `Get context kinds

Get all context kinds for a given project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextsGetContextKindsByProjectKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
