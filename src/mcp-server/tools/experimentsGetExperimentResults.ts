/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsGetExperimentResults } from "../../funcs/experimentsGetExperimentResults.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExperimentResultsRequest$inboundSchema,
};

export const tool$experimentsGetExperimentResults: ToolDefinition<typeof args> =
  {
    name: "experiments-get-experiment-results",
    description: `Get experiment results (Deprecated)

Get results from an experiment for a particular metric.

LaunchDarkly supports one field for expanding the "Get experiment results" response. By default, this field is **not** included in the response.

To expand the response, append the \`expand\` query parameter with the following field:
* \`traffic\` includes the total count of units for each treatment.

For example, \`expand=traffic\` includes the \`traffic\` field for the project in the response.
`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await experimentsGetExperimentResults(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
