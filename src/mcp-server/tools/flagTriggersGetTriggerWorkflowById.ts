/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagTriggersGetTriggerWorkflowById } from "../../funcs/flagTriggersGetTriggerWorkflowById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTriggerWorkflowByIdRequest$inboundSchema,
};

export const tool$flagTriggersGetTriggerWorkflowById: ToolDefinition<
  typeof args
> = {
  name: "flag-triggers-get-trigger-workflow-by-id",
  description: `Get flag trigger by ID

Get a flag trigger by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagTriggersGetTriggerWorkflowById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
