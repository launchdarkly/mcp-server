/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsGetApprovalsForFlag } from "../../funcs/approvalsGetApprovalsForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApprovalsForFlagRequest$inboundSchema,
};

export const tool$approvalsGetApprovalsForFlag: ToolDefinition<typeof args> = {
  name: "approvals-get-approvals-for-flag",
  description: `List approval requests for a flag

Get all approval requests for a feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsGetApprovalsForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
