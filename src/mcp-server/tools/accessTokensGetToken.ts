/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accessTokensGetToken } from "../../funcs/accessTokensGetToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTokenRequest$inboundSchema,
};

export const tool$accessTokensGetToken: ToolDefinition<typeof args> = {
  name: "access-tokens-get-token",
  description: `Get access token

Get a single access token by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accessTokensGetToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
