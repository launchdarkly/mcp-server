/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { announcementsCreateAnnouncementPublic } from "../../funcs/announcementsCreateAnnouncementPublic.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CreateAnnouncementBody$inboundSchema,
};

export const tool$announcementsCreateAnnouncementPublic: ToolDefinition<
  typeof args
> = {
  name: "announcements-create-announcement-public",
  description: `Create an announcement

Create an announcement`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await announcementsCreateAnnouncementPublic(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
