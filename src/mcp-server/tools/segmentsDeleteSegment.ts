/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsDeleteSegment } from "../../funcs/segmentsDeleteSegment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSegmentRequest$inboundSchema,
};

export const tool$segmentsDeleteSegment: ToolDefinition<typeof args> = {
  name: "segments-delete-segment",
  description: `Delete segment

Delete a segment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsDeleteSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
