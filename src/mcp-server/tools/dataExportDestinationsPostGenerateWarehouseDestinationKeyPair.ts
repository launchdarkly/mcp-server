/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataExportDestinationsPostGenerateWarehouseDestinationKeyPair } from "../../funcs/dataExportDestinationsPostGenerateWarehouseDestinationKeyPair.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dataExportDestinationsPostGenerateWarehouseDestinationKeyPair:
  ToolDefinition = {
    name:
      "data-export-destinations-post-generate-warehouse-destination-key-pair",
    description: `Generate Snowflake destination key pair

Generate key pair to allow Data Export to authenticate into a Snowflake warehouse destination`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await dataExportDestinationsPostGenerateWarehouseDestinationKeyPair(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
