/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsCreateBigSegmentExport } from "../../funcs/segmentsCreateBigSegmentExport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateBigSegmentExportRequest$inboundSchema,
};

export const tool$segmentsCreateBigSegmentExport: ToolDefinition<typeof args> =
  {
    name: "segments-create-big-segment-export",
    description: `Create big segment export

Starts a new export process for a big segment. This is an export for a synced segment or a list-based segment that can include more than 15,000 entries.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await segmentsCreateBigSegmentExport(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
