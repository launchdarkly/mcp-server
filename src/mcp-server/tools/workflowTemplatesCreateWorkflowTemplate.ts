/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowTemplatesCreateWorkflowTemplate } from "../../funcs/workflowTemplatesCreateWorkflowTemplate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CreateWorkflowTemplateInput$inboundSchema,
};

export const tool$workflowTemplatesCreateWorkflowTemplate: ToolDefinition<
  typeof args
> = {
  name: "workflow-templates-create-workflow-template",
  description: `Create workflow template

Create a template for a feature flag workflow`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowTemplatesCreateWorkflowTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
