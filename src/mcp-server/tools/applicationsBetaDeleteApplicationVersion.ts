/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsBetaDeleteApplicationVersion } from "../../funcs/applicationsBetaDeleteApplicationVersion.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApplicationVersionRequest$inboundSchema,
};

export const tool$applicationsBetaDeleteApplicationVersion: ToolDefinition<
  typeof args
> = {
  name: "applications-(beta)-delete-application-version",
  description: `Delete application version

Delete an application version.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationsBetaDeleteApplicationVersion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
