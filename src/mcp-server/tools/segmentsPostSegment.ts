/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsPostSegment } from "../../funcs/segmentsPostSegment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostSegmentRequest$inboundSchema,
};

export const tool$segmentsPostSegment: ToolDefinition<typeof args> = {
  name: "segments-post-segment",
  description: `Create segment

Create a new segment.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsPostSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
