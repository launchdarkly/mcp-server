/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagImportConfigurationsBetaGetFlagImportConfigurations } from "../../funcs/flagImportConfigurationsBetaGetFlagImportConfigurations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$flagImportConfigurationsBetaGetFlagImportConfigurations:
  ToolDefinition = {
    name: "flag-import-configurations-(beta)-get-flag-import-configurations",
    description: `List all flag import configurations

List all flag import configurations.`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await flagImportConfigurationsBetaGetFlagImportConfigurations(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
