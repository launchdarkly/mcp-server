/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataExportDestinationsGetDestinations } from "../../funcs/dataExportDestinationsGetDestinations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$dataExportDestinationsGetDestinations: ToolDefinition = {
  name: "data-export-destinations-get-destinations",
  description: `List destinations

Get a list of Data Export destinations configured across all projects and environments.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await dataExportDestinationsGetDestinations(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
