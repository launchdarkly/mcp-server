/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsList } from "../../funcs/approvalsList.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApprovalRequestsRequest$inboundSchema,
};

export const tool$approvalsList: ToolDefinition<typeof args> = {
  name: "list-approval-requests",
  description:
    `Lists all pending approval requests across the account. Enables AI agents to discover outstanding change requests that need review, providing visibility into the approval pipeline before making flag modifications.
`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsList(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
