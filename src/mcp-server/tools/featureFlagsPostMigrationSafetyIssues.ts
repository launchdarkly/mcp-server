/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsPostMigrationSafetyIssues } from "../../funcs/featureFlagsPostMigrationSafetyIssues.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostMigrationSafetyIssuesRequest$inboundSchema,
};

export const tool$featureFlagsPostMigrationSafetyIssues: ToolDefinition<
  typeof args
> = {
  name: "feature-flags-post-migration-safety-issues",
  description: `Get migration safety issues

Returns the migration safety issues that are associated with the POSTed flag patch. The patch must use the semantic patch format for updating feature flags.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsPostMigrationSafetyIssues(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
