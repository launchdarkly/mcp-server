/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextsGetContexts } from "../../funcs/contextsGetContexts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetContextsRequest$inboundSchema,
};

export const tool$contextsGetContexts: ToolDefinition<typeof args> = {
  name: "contexts-get-contexts",
  description: `Get contexts

Get contexts based on kind and key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextsGetContexts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
