/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accessTokensGetTokens } from "../../funcs/accessTokensGetTokens.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTokensRequest$inboundSchema,
};

export const tool$accessTokensGetTokens: ToolDefinition<typeof args> = {
  name: "access-tokens-get-tokens",
  description: `List access tokens

Fetch a list of all access tokens.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accessTokensGetTokens(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
