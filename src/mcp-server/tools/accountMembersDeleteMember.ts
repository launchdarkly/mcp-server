/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountMembersDeleteMember } from "../../funcs/accountMembersDeleteMember.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteMemberRequest$inboundSchema,
};

export const tool$accountMembersDeleteMember: ToolDefinition<typeof args> = {
  name: "account-members-delete-member",
  description: `Delete account member

Delete a single account member by ID. Requests to delete account members will not work if SCIM is enabled for the account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountMembersDeleteMember(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
