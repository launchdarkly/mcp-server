/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsUpdateBigSegmentTargets } from "../../funcs/segmentsUpdateBigSegmentTargets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateBigSegmentTargetsRequest$inboundSchema,
};

export const tool$segmentsUpdateBigSegmentTargets: ToolDefinition<typeof args> =
  {
    name: "segments-update-big-segment-targets",
    description: `Update user context targets on a big segment

Update user context targets included or excluded in a big segment. Big segments include larger list-based segments and synced segments. This operation does not support standard segments.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await segmentsUpdateBigSegmentTargets(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
