/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountUsageBetaGetExperimentationUnitsUsage } from "../../funcs/accountUsageBetaGetExperimentationUnitsUsage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExperimentationUnitsUsageRequest$inboundSchema,
};

export const tool$accountUsageBetaGetExperimentationUnitsUsage: ToolDefinition<
  typeof args
> = {
  name: "account-usage-(beta)-get-experimentation-units-usage",
  description: `Get experimentation units usage

Get a time-series array of the number of monthly experimentation units from your account. The granularity is always daily, with a maximum of 31 days.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await accountUsageBetaGetExperimentationUnitsUsage(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
