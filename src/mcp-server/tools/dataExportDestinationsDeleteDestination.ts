/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataExportDestinationsDeleteDestination } from "../../funcs/dataExportDestinationsDeleteDestination.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDestinationRequest$inboundSchema,
};

export const tool$dataExportDestinationsDeleteDestination: ToolDefinition<
  typeof args
> = {
  name: "data-export-destinations-delete-destination",
  description: `Delete Data Export destination

Delete a Data Export destination by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataExportDestinationsDeleteDestination(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
