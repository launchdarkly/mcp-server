/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { followFlagsPutFlagFollower } from "../../funcs/followFlagsPutFlagFollower.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutFlagFollowerRequest$inboundSchema,
};

export const tool$followFlagsPutFlagFollower: ToolDefinition<typeof args> = {
  name: "follow-flags-put-flag-follower",
  description:
    `Add a member as a follower of a flag in a project and environment

Add a member as a follower to a flag in a project and environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await followFlagsPutFlagFollower(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
