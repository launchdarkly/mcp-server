/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { putOverrideFlag } from "../../funcs/putOverrideFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutOverrideFlagRequest$inboundSchema,
};

export const tool$putOverrideFlag: ToolDefinition<typeof args> = {
  name: "set-feature-flag-value-override",
  description:
    `Sets a local feature flag value override. This facilitates local development and testing in isolation.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await putOverrideFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
