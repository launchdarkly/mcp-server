/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledChangesGetFlagConfigScheduledChanges } from "../../funcs/scheduledChangesGetFlagConfigScheduledChanges.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFlagConfigScheduledChangesRequest$inboundSchema,
};

export const tool$scheduledChangesGetFlagConfigScheduledChanges: ToolDefinition<
  typeof args
> = {
  name: "scheduled-changes-get-flag-config-scheduled-changes",
  description: `List scheduled changes

Get a list of scheduled changes that will be applied to the feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await scheduledChangesGetFlagConfigScheduledChanges(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
