/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextsGetContextAttributeNames } from "../../funcs/contextsGetContextAttributeNames.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetContextAttributeNamesRequest$inboundSchema,
};

export const tool$contextsGetContextAttributeNames: ToolDefinition<
  typeof args
> = {
  name: "contexts-get-context-attribute-names",
  description: `Get context attribute names

Get context attribute names.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextsGetContextAttributeNames(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
