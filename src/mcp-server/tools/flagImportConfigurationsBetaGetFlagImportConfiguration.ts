/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagImportConfigurationsBetaGetFlagImportConfiguration } from "../../funcs/flagImportConfigurationsBetaGetFlagImportConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFlagImportConfigurationRequest$inboundSchema,
};

export const tool$flagImportConfigurationsBetaGetFlagImportConfiguration:
  ToolDefinition<typeof args> = {
    name: "flag-import-configurations-(beta)-get-flag-import-configuration",
    description: `Get a single flag import configuration

Get a single flag import configuration by ID. The \`integrationKey\` path parameter identifies the feature management system from which the import occurs, for example, \`split\`.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await flagImportConfigurationsBetaGetFlagImportConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
