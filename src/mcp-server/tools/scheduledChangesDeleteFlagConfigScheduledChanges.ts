/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledChangesDeleteFlagConfigScheduledChanges } from "../../funcs/scheduledChangesDeleteFlagConfigScheduledChanges.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFlagConfigScheduledChangesRequest$inboundSchema,
};

export const tool$scheduledChangesDeleteFlagConfigScheduledChanges:
  ToolDefinition<typeof args> = {
    name: "scheduled-changes-delete-flag-config-scheduled-changes",
    description: `Delete scheduled changes workflow

Delete a scheduled changes workflow.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await scheduledChangesDeleteFlagConfigScheduledChanges(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
