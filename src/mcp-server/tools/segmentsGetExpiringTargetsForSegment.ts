/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsGetExpiringTargetsForSegment } from "../../funcs/segmentsGetExpiringTargetsForSegment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExpiringTargetsForSegmentRequest$inboundSchema,
};

export const tool$segmentsGetExpiringTargetsForSegment: ToolDefinition<
  typeof args
> = {
  name: "segments-get-expiring-targets-for-segment",
  description: `Get expiring targets for segment

Get a list of a segment's context targets that are scheduled for removal.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsGetExpiringTargetsForSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
