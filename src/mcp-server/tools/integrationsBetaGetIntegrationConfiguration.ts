/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsBetaGetIntegrationConfiguration } from "../../funcs/integrationsBetaGetIntegrationConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetIntegrationConfigurationRequest$inboundSchema,
};

export const tool$integrationsBetaGetIntegrationConfiguration: ToolDefinition<
  typeof args
> = {
  name: "integrations-(beta)-get-integration-configuration",
  description: `Get an integration configuration

Get integration configuration with the specified ID. (Excludes [persistent store](https://launchdarkly.com/docs/api/persistent-store-integrations-beta) and [flag import configurations](https://launchdarkly.com/docs/api/flag-import-configurations-beta).)`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await integrationsBetaGetIntegrationConfiguration(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
