/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { announcementsUpdateAnnouncementPublic } from "../../funcs/announcementsUpdateAnnouncementPublic.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAnnouncementPublicRequest$inboundSchema,
};

export const tool$announcementsUpdateAnnouncementPublic: ToolDefinition<
  typeof args
> = {
  name: "announcements-update-announcement-public",
  description: `Update an announcement

Update an announcement`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await announcementsUpdateAnnouncementPublic(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
