/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { followFlagsGetFollowersByProjEnv } from "../../funcs/followFlagsGetFollowersByProjEnv.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFollowersByProjEnvRequest$inboundSchema,
};

export const tool$followFlagsGetFollowersByProjEnv: ToolDefinition<
  typeof args
> = {
  name: "follow-flags-get-followers-by-proj-env",
  description: `Get followers of all flags in a given project and environment

Get followers of all flags in a given environment and project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await followFlagsGetFollowersByProjEnv(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
