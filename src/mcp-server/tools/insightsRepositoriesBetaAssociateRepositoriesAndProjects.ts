/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsRepositoriesBetaAssociateRepositoriesAndProjects } from "../../funcs/insightsRepositoriesBetaAssociateRepositoriesAndProjects.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.InsightsRepositoryProjectMappings$inboundSchema,
};

export const tool$insightsRepositoriesBetaAssociateRepositoriesAndProjects:
  ToolDefinition<typeof args> = {
    name: "insights-repositories-(beta)-associate-repositories-and-projects",
    description: `Associate repositories with projects

Associate repositories with projects`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await insightsRepositoriesBetaAssociateRepositoriesAndProjects(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
