/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsDeleteApprovalRequest } from "../../funcs/approvalsDeleteApprovalRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApprovalRequestRequest$inboundSchema,
};

export const tool$approvalsDeleteApprovalRequest: ToolDefinition<typeof args> =
  {
    name: "approvals-delete-approval-request",
    description: `Delete approval request

Delete an approval request.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await approvalsDeleteApprovalRequest(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
