/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsBetaDeleteMetricGroup } from "../../funcs/metricsBetaDeleteMetricGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteMetricGroupRequest$inboundSchema,
};

export const tool$metricsBetaDeleteMetricGroup: ToolDefinition<typeof args> = {
  name: "metrics-(beta)-delete-metric-group",
  description: `Delete metric group

Delete a metric group by key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsBetaDeleteMetricGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
