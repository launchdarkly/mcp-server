/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaPostAIConfig } from "../../funcs/aiConfigsBetaPostAIConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostAIConfigRequest$inboundSchema,
};

export const tool$aiConfigsBetaPostAIConfig: ToolDefinition<typeof args> = {
  name: "AI-configs-(beta)-post-AI-config",
  description: `Create new AI Config

Create a new AI Config within the given project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaPostAIConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
