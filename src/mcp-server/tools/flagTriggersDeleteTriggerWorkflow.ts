/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagTriggersDeleteTriggerWorkflow } from "../../funcs/flagTriggersDeleteTriggerWorkflow.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTriggerWorkflowRequest$inboundSchema,
};

export const tool$flagTriggersDeleteTriggerWorkflow: ToolDefinition<
  typeof args
> = {
  name: "flag-triggers-delete-trigger-workflow",
  description: `Delete flag trigger

Delete a flag trigger by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagTriggersDeleteTriggerWorkflow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
