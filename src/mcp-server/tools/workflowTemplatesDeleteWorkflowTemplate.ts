/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowTemplatesDeleteWorkflowTemplate } from "../../funcs/workflowTemplatesDeleteWorkflowTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteWorkflowTemplateRequest$inboundSchema,
};

export const tool$workflowTemplatesDeleteWorkflowTemplate: ToolDefinition<
  typeof args
> = {
  name: "workflow-templates-delete-workflow-template",
  description: `Delete workflow template

Delete a workflow template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowTemplatesDeleteWorkflowTemplate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
