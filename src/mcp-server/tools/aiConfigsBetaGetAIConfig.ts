/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaGetAIConfig } from "../../funcs/aiConfigsBetaGetAIConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAIConfigRequest$inboundSchema,
};

export const tool$aiConfigsBetaGetAIConfig: ToolDefinition<typeof args> = {
  name: "AI-configs-(beta)-get-AI-config",
  description: `Get AI Config

Retrieve a specific AI Config by its key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaGetAIConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
