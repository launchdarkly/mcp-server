/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsGetExperimentResultsForMetricGroup } from "../../funcs/experimentsGetExperimentResultsForMetricGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExperimentResultsForMetricGroupRequest$inboundSchema,
};

export const tool$experimentsGetExperimentResultsForMetricGroup: ToolDefinition<
  typeof args
> = {
  name: "experiments-get-experiment-results-for-metric-group",
  description: `Get experiment results for metric group (Deprecated)

Get results from an experiment for a particular metric group.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await experimentsGetExperimentResultsForMetricGroup(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
