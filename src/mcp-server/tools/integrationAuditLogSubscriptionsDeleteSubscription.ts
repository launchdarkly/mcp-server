/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationAuditLogSubscriptionsDeleteSubscription } from "../../funcs/integrationAuditLogSubscriptionsDeleteSubscription.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteSubscriptionRequest$inboundSchema,
};

export const tool$integrationAuditLogSubscriptionsDeleteSubscription:
  ToolDefinition<typeof args> = {
    name: "integration-audit-log-subscriptions-delete-subscription",
    description: `Delete audit log subscription

Delete an audit log subscription.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await integrationAuditLogSubscriptionsDeleteSubscription(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
