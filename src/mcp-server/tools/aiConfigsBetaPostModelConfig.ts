/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaPostModelConfig } from "../../funcs/aiConfigsBetaPostModelConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostModelConfigRequest$inboundSchema,
};

export const tool$aiConfigsBetaPostModelConfig: ToolDefinition<typeof args> = {
  name: "AI-configs-(beta)-post-model-config",
  description: `Create an AI model config

Create an AI model config. You can use this in any variation for any AI Config in your project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaPostModelConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
