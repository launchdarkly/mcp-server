/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationsBetaDeleteIntegrationConfiguration } from "../../funcs/integrationsBetaDeleteIntegrationConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteIntegrationConfigurationRequest$inboundSchema,
};

export const tool$integrationsBetaDeleteIntegrationConfiguration:
  ToolDefinition<typeof args> = {
    name: "integrations-(beta)-delete-integration-configuration",
    description: `Delete integration configuration

Delete an integration configuration by ID. (Excludes [persistent store](https://launchdarkly.com/docs/api/persistent-store-integrations-beta) and [flag import configurations](https://launchdarkly.com/docs/api/flag-import-configurations-beta).)`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await integrationsBetaDeleteIntegrationConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
