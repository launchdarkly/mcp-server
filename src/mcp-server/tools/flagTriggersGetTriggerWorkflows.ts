/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagTriggersGetTriggerWorkflows } from "../../funcs/flagTriggersGetTriggerWorkflows.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTriggerWorkflowsRequest$inboundSchema,
};

export const tool$flagTriggersGetTriggerWorkflows: ToolDefinition<typeof args> =
  {
    name: "flag-triggers-get-trigger-workflows",
    description: `List flag triggers

Get a list of all flag triggers.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await flagTriggersGetTriggerWorkflows(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
