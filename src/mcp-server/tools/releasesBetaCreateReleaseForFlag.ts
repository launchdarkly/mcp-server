/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { releasesBetaCreateReleaseForFlag } from "../../funcs/releasesBetaCreateReleaseForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateReleaseForFlagRequest$inboundSchema,
};

export const tool$releasesBetaCreateReleaseForFlag: ToolDefinition<
  typeof args
> = {
  name: "releases-(beta)-create-release-for-flag",
  description: `Create a new release for flag

Creates a release by adding a flag to a release pipeline`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await releasesBetaCreateReleaseForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
