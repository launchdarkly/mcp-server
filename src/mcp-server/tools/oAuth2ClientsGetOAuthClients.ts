/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuth2ClientsGetOAuthClients } from "../../funcs/oAuth2ClientsGetOAuthClients.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$oAuth2ClientsGetOAuthClients: ToolDefinition = {
  name: "O-auth2-clients-get-O-auth-clients",
  description: `Get clients

Get all OAuth 2.0 clients registered by your account.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await oAuth2ClientsGetOAuthClients(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
