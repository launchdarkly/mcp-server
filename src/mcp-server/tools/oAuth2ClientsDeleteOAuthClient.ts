/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuth2ClientsDeleteOAuthClient } from "../../funcs/oAuth2ClientsDeleteOAuthClient.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOAuthClientRequest$inboundSchema,
};

export const tool$oAuth2ClientsDeleteOAuthClient: ToolDefinition<typeof args> =
  {
    name: "O-auth2-clients-delete-O-auth-client",
    description: `Delete OAuth 2.0 client

Delete an existing OAuth 2.0 client by unique client ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await oAuth2ClientsDeleteOAuthClient(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
