/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { releasesBetaUpdatePhaseStatus } from "../../funcs/releasesBetaUpdatePhaseStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdatePhaseStatusRequest$inboundSchema,
};

export const tool$releasesBetaUpdatePhaseStatus: ToolDefinition<typeof args> = {
  name: "releases-(beta)-update-phase-status",
  description: `Update phase status for release

Updates the execution status of a phase of a release`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await releasesBetaUpdatePhaseStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
