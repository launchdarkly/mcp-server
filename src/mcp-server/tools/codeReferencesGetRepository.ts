/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesGetRepository } from "../../funcs/codeReferencesGetRepository.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetRepositoryRequest$inboundSchema,
};

export const tool$codeReferencesGetRepository: ToolDefinition<typeof args> = {
  name: "code-references-get-repository",
  description: `Get repository

Get a single repository by name.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesGetRepository(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
