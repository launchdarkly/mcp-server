/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsReviewFlagRequest } from "../../funcs/approvalsReviewFlagRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApprovalRequestReviewForFlagRequest$inboundSchema,
};

export const tool$approvalsReviewFlagRequest: ToolDefinition<typeof args> = {
  name: "review-flag-approval-request",
  description:
    `Submits a review (approve, decline, or comment) on a pending approval request for a feature flag. Enables agents to participate in the approval workflow by providing feedback or granting approval for proposed changes.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsReviewFlagRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
