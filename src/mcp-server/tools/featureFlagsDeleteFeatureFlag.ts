/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsDeleteFeatureFlag } from "../../funcs/featureFlagsDeleteFeatureFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFeatureFlagRequest$inboundSchema,
};

export const tool$featureFlagsDeleteFeatureFlag: ToolDefinition<typeof args> = {
  name: "feature-flags-delete-feature-flag",
  description: `Delete feature flag

Delete a feature flag in all environments. Use with caution: only delete feature flags your application no longer uses.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsDeleteFeatureFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
