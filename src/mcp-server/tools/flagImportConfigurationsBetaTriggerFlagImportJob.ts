/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagImportConfigurationsBetaTriggerFlagImportJob } from "../../funcs/flagImportConfigurationsBetaTriggerFlagImportJob.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TriggerFlagImportJobRequest$inboundSchema,
};

export const tool$flagImportConfigurationsBetaTriggerFlagImportJob:
  ToolDefinition<typeof args> = {
    name: "flag-import-configurations-(beta)-trigger-flag-import-job",
    description: `Trigger a single flag import run

Trigger a single flag import run for an existing flag import configuration. The \`integrationKey\` path parameter identifies the feature management system from which the import occurs, for example, \`split\`.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await flagImportConfigurationsBetaTriggerFlagImportJob(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
