/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaDeleteModelConfig } from "../../funcs/aiConfigsBetaDeleteModelConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteModelConfigRequest$inboundSchema,
};

export const tool$aiConfigsBetaDeleteModelConfig: ToolDefinition<typeof args> =
  {
    name: "AI-configs-(beta)-delete-model-config",
    description: `Delete an AI model config

Delete an AI model config.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await aiConfigsBetaDeleteModelConfig(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
