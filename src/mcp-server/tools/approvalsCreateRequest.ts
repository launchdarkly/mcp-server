/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsCreateRequest } from "../../funcs/approvalsCreateRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApprovalRequestForFlagRequest$inboundSchema,
};

export const tool$approvalsCreateRequest: ToolDefinition<typeof args> = {
  name: "create-flag-approval-request",
  description:
    `Creates an approval request for changes to a feature flag's targeting or variations in a specific environment. Required when the environment has mandatory approvals enabled, allowing agents to submit changes for review instead of applying them directly.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsCreateRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
