/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsPostProject } from "../../funcs/projectsPostProject.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.ProjectPost$inboundSchema,
};

export const tool$projectsPostProject: ToolDefinition<typeof args> = {
  name: "projects-post-project",
  description: `Create project

Create a new project with the given key and name. Project keys must be unique within an account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsPostProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
