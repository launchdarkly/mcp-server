/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsGetExperimentationSettings } from "../../funcs/experimentsGetExperimentationSettings.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExperimentationSettingsRequest$inboundSchema,
};

export const tool$experimentsGetExperimentationSettings: ToolDefinition<
  typeof args
> = {
  name: "experiments-get-experimentation-settings",
  description: `Get experimentation settings

Get current experimentation settings for the given project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsGetExperimentationSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
