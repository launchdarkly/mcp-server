/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsGetSegmentMembershipForContext } from "../../funcs/segmentsGetSegmentMembershipForContext.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSegmentMembershipForContextRequest$inboundSchema,
};

export const tool$segmentsGetSegmentMembershipForContext: ToolDefinition<
  typeof args
> = {
  name: "segments-get-segment-membership-for-context",
  description: `Get big segment membership for context

Get the membership status (included/excluded) for a given context in this big segment. Big segments include larger list-based segments and synced segments. This operation does not support standard segments.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsGetSegmentMembershipForContext(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
