/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsChartsBetaGetFlagStatusChart } from "../../funcs/insightsChartsBetaGetFlagStatusChart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFlagStatusChartRequest$inboundSchema,
};

export const tool$insightsChartsBetaGetFlagStatusChart: ToolDefinition<
  typeof args
> = {
  name: "insights-charts-(beta)-get-flag-status-chart",
  description: `Get flag status chart data

Get flag status chart data. To learn more, read [Flag statuses](https://launchdarkly.com/docs/home/observability/flag-health#flag-statuses).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsChartsBetaGetFlagStatusChart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
