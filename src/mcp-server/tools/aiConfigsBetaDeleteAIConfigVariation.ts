/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaDeleteAIConfigVariation } from "../../funcs/aiConfigsBetaDeleteAIConfigVariation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAIConfigVariationRequest$inboundSchema,
};

export const tool$aiConfigsBetaDeleteAIConfigVariation: ToolDefinition<
  typeof args
> = {
  name: "AI-configs-(beta)-delete-AI-config-variation",
  description: `Delete AI Config variation

Delete a specific variation of an AI Config by config key and variation key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaDeleteAIConfigVariation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
