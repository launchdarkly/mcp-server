/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { relayProxyConfigurationsPostRelayAutoConfig } from "../../funcs/relayProxyConfigurationsPostRelayAutoConfig.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.RelayAutoConfigPost$inboundSchema,
};

export const tool$relayProxyConfigurationsPostRelayAutoConfig: ToolDefinition<
  typeof args
> = {
  name: "relay-proxy-configurations-post-relay-auto-config",
  description: `Create a new Relay Proxy config

Create a Relay Proxy config.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await relayProxyConfigurationsPostRelayAutoConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
