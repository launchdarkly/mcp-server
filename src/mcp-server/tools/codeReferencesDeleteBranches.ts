/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesDeleteBranches } from "../../funcs/codeReferencesDeleteBranches.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteBranchesRequest$inboundSchema,
};

export const tool$codeReferencesDeleteBranches: ToolDefinition<typeof args> = {
  name: "code-references-delete-branches",
  description: `Delete branches

Asynchronously delete a number of branches.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesDeleteBranches(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
