/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsDeleteEnvironment } from "../../funcs/environmentsDeleteEnvironment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteEnvironmentRequest$inboundSchema,
};

export const tool$environmentsDeleteEnvironment: ToolDefinition<typeof args> = {
  name: "environments-delete-environment",
  description: `Delete environment

Delete a environment by key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsDeleteEnvironment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
