/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetAllWebhooks } from "../../funcs/webhooksGetAllWebhooks.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$webhooksGetAllWebhooks: ToolDefinition = {
  name: "webhooks-get-all-webhooks",
  description: `List webhooks

Fetch a list of all webhooks.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await webhooksGetAllWebhooks(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
