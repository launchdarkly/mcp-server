/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsBetaPatchApplicationVersion } from "../../funcs/applicationsBetaPatchApplicationVersion.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchApplicationVersionRequest$inboundSchema,
};

export const tool$applicationsBetaPatchApplicationVersion: ToolDefinition<
  typeof args
> = {
  name: "applications-(beta)-patch-application-version",
  description: `Update application version

Update an application version. You can update the \`supported\` field. Requires a [JSON patch](https://datatracker.ietf.org/doc/html/rfc6902) representation of the desired changes to the application version. To learn more, read [Updates](https://launchdarkly.com/docs/api#updates).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationsBetaPatchApplicationVersion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
