/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaGetAIConfigMetrics } from "../../funcs/aiConfigsBetaGetAIConfigMetrics.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAIConfigMetricsRequest$inboundSchema,
};

export const tool$aiConfigsBetaGetAIConfigMetrics: ToolDefinition<typeof args> =
  {
    name: "AI-configs-(beta)-get-AI-config-metrics",
    description: `Get AI Config metrics

Retrieve usage metrics for an AI Config by config key.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await aiConfigsBetaGetAIConfigMetrics(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
