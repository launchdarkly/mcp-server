/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowsDeleteWorkflow } from "../../funcs/workflowsDeleteWorkflow.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteWorkflowRequest$inboundSchema,
};

export const tool$workflowsDeleteWorkflow: ToolDefinition<typeof args> = {
  name: "workflows-delete-workflow",
  description: `Delete workflow

Delete a workflow from a feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsDeleteWorkflow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
