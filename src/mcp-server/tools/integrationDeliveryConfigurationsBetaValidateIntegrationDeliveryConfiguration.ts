/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationDeliveryConfigurationsBetaValidateIntegrationDeliveryConfiguration } from "../../funcs/integrationDeliveryConfigurationsBetaValidateIntegrationDeliveryConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.ValidateIntegrationDeliveryConfigurationRequest$inboundSchema,
};

export const tool$integrationDeliveryConfigurationsBetaValidateIntegrationDeliveryConfiguration:
  ToolDefinition<typeof args> = {
    name:
      "integration-delivery-configurations-(beta)-validate-integration-delivery-configuration",
    description: `Validate delivery configuration

Validate the saved delivery configuration, using the \`validationRequest\` in the integration's \`manifest.json\` file.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await integrationDeliveryConfigurationsBetaValidateIntegrationDeliveryConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
