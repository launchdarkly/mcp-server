/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsBetaPatchMetricGroup } from "../../funcs/metricsBetaPatchMetricGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchMetricGroupRequest$inboundSchema,
};

export const tool$metricsBetaPatchMetricGroup: ToolDefinition<typeof args> = {
  name: "metrics-(beta)-patch-metric-group",
  description: `Patch metric group

Patch a metric group by key. Updating a metric group uses a [JSON patch](https://datatracker.ietf.org/doc/html/rfc6902) representation of the desired changes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsBetaPatchMetricGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
