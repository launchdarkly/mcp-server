/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customRolesDeleteCustomRole } from "../../funcs/customRolesDeleteCustomRole.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCustomRoleRequest$inboundSchema,
};

export const tool$customRolesDeleteCustomRole: ToolDefinition<typeof args> = {
  name: "custom-roles-delete-custom-role",
  description: `Delete custom role

Delete a custom role by key`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customRolesDeleteCustomRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
