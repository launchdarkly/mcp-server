/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsGetFeatureFlagStatus } from "../../funcs/featureFlagsGetFeatureFlagStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFeatureFlagStatusRequest$inboundSchema,
};

export const tool$featureFlagsGetFeatureFlagStatus: ToolDefinition<
  typeof args
> = {
  name: "feature-flags-get-feature-flag-status",
  description: `Get feature flag status

Get the status for a particular feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsGetFeatureFlagStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
