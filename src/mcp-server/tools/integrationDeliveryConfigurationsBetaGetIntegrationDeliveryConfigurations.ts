/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationDeliveryConfigurationsBetaGetIntegrationDeliveryConfigurations } from "../../funcs/integrationDeliveryConfigurationsBetaGetIntegrationDeliveryConfigurations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$integrationDeliveryConfigurationsBetaGetIntegrationDeliveryConfigurations:
  ToolDefinition = {
    name:
      "integration-delivery-configurations-(beta)-get-integration-delivery-configurations",
    description: `List all delivery configurations

List all delivery configurations.`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await integrationDeliveryConfigurationsBetaGetIntegrationDeliveryConfigurations(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
