/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesGetBranch } from "../../funcs/codeReferencesGetBranch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBranchRequest$inboundSchema,
};

export const tool$codeReferencesGetBranch: ToolDefinition<typeof args> = {
  name: "code-references-get-branch",
  description: `Get branch

Get a specific branch in a repository.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesGetBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
