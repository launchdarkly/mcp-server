/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsGetTeamMaintainers } from "../../funcs/teamsGetTeamMaintainers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeamMaintainersRequest$inboundSchema,
};

export const tool$teamsGetTeamMaintainers: ToolDefinition<typeof args> = {
  name: "teams-get-team-maintainers",
  description: `Get team maintainers

Fetch the maintainers that have been assigned to the team. To learn more, read [Managing team maintainers](https://launchdarkly.com/docs/home/account/team-maintainers).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsGetTeamMaintainers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
