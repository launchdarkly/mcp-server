/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accessTokensDeleteToken } from "../../funcs/accessTokensDeleteToken.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTokenRequest$inboundSchema,
};

export const tool$accessTokensDeleteToken: ToolDefinition<typeof args> = {
  name: "access-tokens-delete-token",
  description: `Delete access token

Delete an access token by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accessTokensDeleteToken(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
