/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsChartsBetaGetLeadTimeChart } from "../../funcs/insightsChartsBetaGetLeadTimeChart.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLeadTimeChartRequest$inboundSchema,
};

export const tool$insightsChartsBetaGetLeadTimeChart: ToolDefinition<
  typeof args
> = {
  name: "insights-charts-(beta)-get-lead-time-chart",
  description: `Get lead time chart data

Get lead time chart data. The engineering insights UI displays lead time data in the [lead time metric view](https://launchdarkly.com/docs/home/observability/lead-time).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsChartsBetaGetLeadTimeChart(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
