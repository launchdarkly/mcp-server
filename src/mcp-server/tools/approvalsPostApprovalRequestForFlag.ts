/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsPostApprovalRequestForFlag } from "../../funcs/approvalsPostApprovalRequestForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApprovalRequestForFlagRequest$inboundSchema,
};

export const tool$approvalsPostApprovalRequestForFlag: ToolDefinition<
  typeof args
> = {
  name: "approvals-post-approval-request-for-flag",
  description: `Create approval request for a flag

Create an approval request for a feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsPostApprovalRequestForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
