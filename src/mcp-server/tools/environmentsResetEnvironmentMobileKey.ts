/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsResetEnvironmentMobileKey } from "../../funcs/environmentsResetEnvironmentMobileKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ResetEnvironmentMobileKeyRequest$inboundSchema,
};

export const tool$environmentsResetEnvironmentMobileKey: ToolDefinition<
  typeof args
> = {
  name: "environments-reset-environment-mobile-key",
  description: `Reset environment mobile SDK key

Reset an environment's mobile key. The optional expiry for the old key is deprecated for this endpoint, so the old key will always expire immediately.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsResetEnvironmentMobileKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
