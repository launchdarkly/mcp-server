/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsGetContextInstanceSegmentsMembershipByEnv } from "../../funcs/segmentsGetContextInstanceSegmentsMembershipByEnv.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetContextInstanceSegmentsMembershipByEnvRequest$inboundSchema,
};

export const tool$segmentsGetContextInstanceSegmentsMembershipByEnv:
  ToolDefinition<typeof args> = {
    name: "segments-get-context-instance-segments-membership-by-env",
    description: `List segment memberships for context instance

For a given context instance with attributes, get membership details for all segments. In the request body, pass in the context instance.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await segmentsGetContextInstanceSegmentsMembershipByEnv(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
