/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationAuditLogSubscriptionsGetSubscriptionByID } from "../../funcs/integrationAuditLogSubscriptionsGetSubscriptionByID.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSubscriptionByIDRequest$inboundSchema,
};

export const tool$integrationAuditLogSubscriptionsGetSubscriptionByID:
  ToolDefinition<typeof args> = {
    name: "integration-audit-log-subscriptions-get-subscription-by-ID",
    description: `Get audit log subscription by ID

Get an audit log subscription by ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await integrationAuditLogSubscriptionsGetSubscriptionByID(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
