/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsGetFeatureFlagStatusAcrossEnvironments } from "../../funcs/featureFlagsGetFeatureFlagStatusAcrossEnvironments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetFeatureFlagStatusAcrossEnvironmentsRequest$inboundSchema,
};

export const tool$featureFlagsGetFeatureFlagStatusAcrossEnvironments:
  ToolDefinition<typeof args> = {
    name: "feature-flags-get-feature-flag-status-across-environments",
    description: `Get flag status across environments

Get the status for a particular feature flag across environments.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await featureFlagsGetFeatureFlagStatusAcrossEnvironments(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
