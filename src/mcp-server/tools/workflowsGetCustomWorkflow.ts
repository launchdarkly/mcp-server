/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workflowsGetCustomWorkflow } from "../../funcs/workflowsGetCustomWorkflow.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCustomWorkflowRequest$inboundSchema,
};

export const tool$workflowsGetCustomWorkflow: ToolDefinition<typeof args> = {
  name: "workflows-get-custom-workflow",
  description: `Get custom workflow

Get a specific workflow by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await workflowsGetCustomWorkflow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
