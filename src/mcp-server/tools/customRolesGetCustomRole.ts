/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customRolesGetCustomRole } from "../../funcs/customRolesGetCustomRole.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetCustomRoleRequest$inboundSchema,
};

export const tool$customRolesGetCustomRole: ToolDefinition<typeof args> = {
  name: "custom-roles-get-custom-role",
  description: `Get custom role

Get a single custom role by key or ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customRolesGetCustomRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
