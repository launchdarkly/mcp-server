/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuth2ClientsGetOAuthClientById } from "../../funcs/oAuth2ClientsGetOAuthClientById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetOAuthClientByIdRequest$inboundSchema,
};

export const tool$oAuth2ClientsGetOAuthClientById: ToolDefinition<typeof args> =
  {
    name: "O-auth2-clients-get-O-auth-client-by-id",
    description: `Get client by ID

Get a registered OAuth 2.0 client by unique client ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await oAuth2ClientsGetOAuthClientById(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
