/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { projectsPutFlagDefaultsByProject } from "../../funcs/projectsPutFlagDefaultsByProject.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutFlagDefaultsByProjectRequest$inboundSchema,
};

export const tool$projectsPutFlagDefaultsByProject: ToolDefinition<
  typeof args
> = {
  name: "projects-put-flag-defaults-by-project",
  description: `Create or update flag defaults for project

Create or update flag defaults for a project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await projectsPutFlagDefaultsByProject(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
