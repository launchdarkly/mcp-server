/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsScoresBetaDeleteInsightGroup } from "../../funcs/insightsScoresBetaDeleteInsightGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteInsightGroupRequest$inboundSchema,
};

export const tool$insightsScoresBetaDeleteInsightGroup: ToolDefinition<
  typeof args
> = {
  name: "insights-scores-(beta)-delete-insight-group",
  description: `Delete insight group

Delete insight group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsScoresBetaDeleteInsightGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
