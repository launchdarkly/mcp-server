/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaDeleteRestrictedModels } from "../../funcs/aiConfigsBetaDeleteRestrictedModels.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteRestrictedModelsRequest$inboundSchema,
};

export const tool$aiConfigsBetaDeleteRestrictedModels: ToolDefinition<
  typeof args
> = {
  name: "AI-configs-(beta)-delete-restricted-models",
  description: `Remove AI models from the restricted list

Remove AI models, by key, from the restricted list.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaDeleteRestrictedModels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
