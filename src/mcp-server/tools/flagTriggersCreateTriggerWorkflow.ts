/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagTriggersCreateTriggerWorkflow } from "../../funcs/flagTriggersCreateTriggerWorkflow.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateTriggerWorkflowRequest$inboundSchema,
};

export const tool$flagTriggersCreateTriggerWorkflow: ToolDefinition<
  typeof args
> = {
  name: "flag-triggers-create-trigger-workflow",
  description: `Create flag trigger

Create a new flag trigger.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagTriggersCreateTriggerWorkflow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
