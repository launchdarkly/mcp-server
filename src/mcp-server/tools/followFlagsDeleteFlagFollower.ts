/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { followFlagsDeleteFlagFollower } from "../../funcs/followFlagsDeleteFlagFollower.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFlagFollowerRequest$inboundSchema,
};

export const tool$followFlagsDeleteFlagFollower: ToolDefinition<typeof args> = {
  name: "follow-flags-delete-flag-follower",
  description:
    `Remove a member as a follower of a flag in a project and environment

Remove a member as a follower to a flag in a project and environment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await followFlagsDeleteFlagFollower(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
