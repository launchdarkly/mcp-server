/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featureFlagsGetStatus } from "../../funcs/featureFlagsGetStatus.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetFeatureFlagStatusAcrossEnvironmentsRequest$inboundSchema,
};

export const tool$featureFlagsGetStatus: ToolDefinition<typeof args> = {
  name: "get-flag-status-across-environments",
  description:
    `Retrieves the lifecycle status of a feature flag across all environments, including usage patterns, evaluation counts, and stability indicators. Essential for determining flag removal readiness based on actual production data.
`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await featureFlagsGetStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
