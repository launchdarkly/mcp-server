/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { userSettingsGetExpiringFlagsForUser } from "../../funcs/userSettingsGetExpiringFlagsForUser.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExpiringFlagsForUserRequest$inboundSchema,
};

export const tool$userSettingsGetExpiringFlagsForUser: ToolDefinition<
  typeof args
> = {
  name: "user-settings-get-expiring-flags-for-user",
  description: `Get expiring dates on flags for user

Get a list of flags for which the given user is scheduled for removal.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await userSettingsGetExpiringFlagsForUser(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
