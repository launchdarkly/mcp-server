/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { teamsGetTeamRoles } from "../../funcs/teamsGetTeamRoles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTeamRolesRequest$inboundSchema,
};

export const tool$teamsGetTeamRoles: ToolDefinition<typeof args> = {
  name: "teams-get-team-roles",
  description: `Get team custom roles

Fetch the custom roles that have been assigned to the team. To learn more, read [Managing team permissions](https://launchdarkly.com/docs/home/account/team-permissions).`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await teamsGetTeamRoles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
