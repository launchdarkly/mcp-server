/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsUpdateBigSegmentContextTargets } from "../../funcs/segmentsUpdateBigSegmentContextTargets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateBigSegmentContextTargetsRequest$inboundSchema,
};

export const tool$segmentsUpdateBigSegmentContextTargets: ToolDefinition<
  typeof args
> = {
  name: "segments-update-big-segment-context-targets",
  description: `Update context targets on a big segment

Update context targets included or excluded in a big segment. Big segments include larger list-based segments and synced segments. This operation does not support standard segments.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsUpdateBigSegmentContextTargets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
