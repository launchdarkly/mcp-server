/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsGetBigSegmentExport } from "../../funcs/segmentsGetBigSegmentExport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBigSegmentExportRequest$inboundSchema,
};

export const tool$segmentsGetBigSegmentExport: ToolDefinition<typeof args> = {
  name: "segments-get-big-segment-export",
  description: `Get big segment export

Returns information about a big segment export process. This is an export for a synced segment or a list-based segment that can include more than 15,000 entries.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsGetBigSegmentExport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
