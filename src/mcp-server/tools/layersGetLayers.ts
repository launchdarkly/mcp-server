/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { layersGetLayers } from "../../funcs/layersGetLayers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetLayersRequest$inboundSchema,
};

export const tool$layersGetLayers: ToolDefinition<typeof args> = {
  name: "layers-get-layers",
  description: `Get layers

Get a collection of all layers for a project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layersGetLayers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
