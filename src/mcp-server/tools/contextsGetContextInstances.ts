/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextsGetContextInstances } from "../../funcs/contextsGetContextInstances.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetContextInstancesRequest$inboundSchema,
};

export const tool$contextsGetContextInstances: ToolDefinition<typeof args> = {
  name: "contexts-get-context-instances",
  description: `Get context instances

Get context instances by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextsGetContextInstances(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
