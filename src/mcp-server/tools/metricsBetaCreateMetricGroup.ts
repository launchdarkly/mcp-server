/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metricsBetaCreateMetricGroup } from "../../funcs/metricsBetaCreateMetricGroup.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateMetricGroupRequest$inboundSchema,
};

export const tool$metricsBetaCreateMetricGroup: ToolDefinition<typeof args> = {
  name: "metrics-(beta)-create-metric-group",
  description: `Create metric group

Create a new metric group in the specified project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await metricsBetaCreateMetricGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
