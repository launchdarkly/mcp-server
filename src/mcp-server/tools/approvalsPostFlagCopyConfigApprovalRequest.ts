/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsPostFlagCopyConfigApprovalRequest } from "../../funcs/approvalsPostFlagCopyConfigApprovalRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostFlagCopyConfigApprovalRequestRequest$inboundSchema,
};

export const tool$approvalsPostFlagCopyConfigApprovalRequest: ToolDefinition<
  typeof args
> = {
  name: "approvals-post-flag-copy-config-approval-request",
  description:
    `Create approval request to copy flag configurations across environments

Create an approval request to copy a feature flag's configuration across environments.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsPostFlagCopyConfigApprovalRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
