/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesGetRootStatistic } from "../../funcs/codeReferencesGetRootStatistic.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$codeReferencesGetRootStatistic: ToolDefinition = {
  name: "code-references-get-root-statistic",
  description: `Get links to code reference repositories for each project

Get links for all projects that have code references.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await codeReferencesGetRootStatistic(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
