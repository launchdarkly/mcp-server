/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsGetApprovalForFlag } from "../../funcs/approvalsGetApprovalForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApprovalForFlagRequest$inboundSchema,
};

export const tool$approvalsGetApprovalForFlag: ToolDefinition<typeof args> = {
  name: "approvals-get-approval-for-flag",
  description: `Get approval request for a flag

Get a single approval request for a feature flag.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsGetApprovalForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
