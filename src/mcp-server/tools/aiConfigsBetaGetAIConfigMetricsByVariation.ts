/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaGetAIConfigMetricsByVariation } from "../../funcs/aiConfigsBetaGetAIConfigMetricsByVariation.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAIConfigMetricsByVariationRequest$inboundSchema,
};

export const tool$aiConfigsBetaGetAIConfigMetricsByVariation: ToolDefinition<
  typeof args
> = {
  name: "AI-configs-(beta)-get-AI-config-metrics-by-variation",
  description: `Get AI Config metrics by variation

Retrieve usage metrics for an AI Config by config key, with results split by variation.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaGetAIConfigMetricsByVariation(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
