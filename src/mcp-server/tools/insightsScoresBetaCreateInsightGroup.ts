/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsScoresBetaCreateInsightGroup } from "../../funcs/insightsScoresBetaCreateInsightGroup.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.PostInsightGroupParams$inboundSchema,
};

export const tool$insightsScoresBetaCreateInsightGroup: ToolDefinition<
  typeof args
> = {
  name: "insights-scores-(beta)-create-insight-group",
  description: `Create insight group

Create insight group`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsScoresBetaCreateInsightGroup(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
