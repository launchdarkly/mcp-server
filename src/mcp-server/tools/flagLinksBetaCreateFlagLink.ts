/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagLinksBetaCreateFlagLink } from "../../funcs/flagLinksBetaCreateFlagLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateFlagLinkRequest$inboundSchema,
};

export const tool$flagLinksBetaCreateFlagLink: ToolDefinition<typeof args> = {
  name: "flag-links-(beta)-create-flag-link",
  description: `Create flag link

Create a new flag link. Flag links let you reference external resources and associate them with your flags.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagLinksBetaCreateFlagLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
