/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { announcementsDeleteAnnouncementPublic } from "../../funcs/announcementsDeleteAnnouncementPublic.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteAnnouncementPublicRequest$inboundSchema,
};

export const tool$announcementsDeleteAnnouncementPublic: ToolDefinition<
  typeof args
> = {
  name: "announcements-delete-announcement-public",
  description: `Delete an announcement

Delete an announcement`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await announcementsDeleteAnnouncementPublic(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
