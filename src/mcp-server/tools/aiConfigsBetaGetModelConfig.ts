/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaGetModelConfig } from "../../funcs/aiConfigsBetaGetModelConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetModelConfigRequest$inboundSchema,
};

export const tool$aiConfigsBetaGetModelConfig: ToolDefinition<typeof args> = {
  name: "AI-configs-(beta)-get-model-config",
  description: `Get AI model config

Get an AI model config by key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaGetModelConfig(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
