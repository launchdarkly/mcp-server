/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { integrationDeliveryConfigurationsBetaPatchIntegrationDeliveryConfiguration } from "../../funcs/integrationDeliveryConfigurationsBetaPatchIntegrationDeliveryConfiguration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.PatchIntegrationDeliveryConfigurationRequest$inboundSchema,
};

export const tool$integrationDeliveryConfigurationsBetaPatchIntegrationDeliveryConfiguration:
  ToolDefinition<typeof args> = {
    name:
      "integration-delivery-configurations-(beta)-patch-integration-delivery-configuration",
    description: `Update delivery configuration

Update an integration delivery configuration. Updating an integration delivery configuration uses a [JSON patch](https://datatracker.ietf.org/doc/html/rfc6902) representation of the desired changes. To learn more, read [Updates](https://launchdarkly.com/docs/api#updates).`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await integrationDeliveryConfigurationsBetaPatchIntegrationDeliveryConfiguration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
