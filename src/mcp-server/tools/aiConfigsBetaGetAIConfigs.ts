/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { aiConfigsBetaGetAIConfigs } from "../../funcs/aiConfigsBetaGetAIConfigs.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAIConfigsRequest$inboundSchema,
};

export const tool$aiConfigsBetaGetAIConfigs: ToolDefinition<typeof args> = {
  name: "AI-configs-(beta)-get-AI-configs",
  description: `List AI Configs

Get a list of all AI Configs in the given project.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await aiConfigsBetaGetAIConfigs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
