/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesPostRepository } from "../../funcs/codeReferencesPostRepository.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.RepositoryPost$inboundSchema,
};

export const tool$codeReferencesPostRepository: ToolDefinition<typeof args> = {
  name: "code-references-post-repository",
  description: `Create repository

Create a repository with the specified name.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesPostRepository(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
