/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { releasesBetaGetReleaseByFlagKey } from "../../funcs/releasesBetaGetReleaseByFlagKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetReleaseByFlagKeyRequest$inboundSchema,
};

export const tool$releasesBetaGetReleaseByFlagKey: ToolDefinition<typeof args> =
  {
    name: "releases-(beta)-get-release-by-flag-key",
    description: `Get release for flag

Get currently active release for a flag`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await releasesBetaGetReleaseByFlagKey(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
