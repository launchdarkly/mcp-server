/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deleteOverrides } from "../../funcs/deleteOverrides.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteOverridesRequest$inboundSchema,
};

export const tool$deleteOverrides: ToolDefinition<typeof args> = {
  name: "delete-feature-flag-value-overrides",
  description:
    `Deletes all local feature flag value overrides for a specified project. This resets all feature flags in the project to their original values.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deleteOverrides(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
