/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { accountMembersPostMemberTeams } from "../../funcs/accountMembersPostMemberTeams.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostMemberTeamsRequest$inboundSchema,
};

export const tool$accountMembersPostMemberTeams: ToolDefinition<typeof args> = {
  name: "account-members-post-member-teams",
  description: `Add a member to teams

Add one member to one or more teams.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await accountMembersPostMemberTeams(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
