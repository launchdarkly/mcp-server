/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { releasePipelinesBetaPutReleasePipeline } from "../../funcs/releasePipelinesBetaPutReleasePipeline.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutReleasePipelineRequest$inboundSchema,
};

export const tool$releasePipelinesBetaPutReleasePipeline: ToolDefinition<
  typeof args
> = {
  name: "release-pipelines-(beta)-put-release-pipeline",
  description: `Update a release pipeline

Updates a release pipeline.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await releasePipelinesBetaPutReleasePipeline(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
