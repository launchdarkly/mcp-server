/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesGetBranches } from "../../funcs/codeReferencesGetBranches.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBranchesRequest$inboundSchema,
};

export const tool$codeReferencesGetBranches: ToolDefinition<typeof args> = {
  name: "code-references-get-branches",
  description: `List branches

Get a list of branches.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesGetBranches(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
