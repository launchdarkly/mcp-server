/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customRolesPostCustomRole } from "../../funcs/customRolesPostCustomRole.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CustomRolePost$inboundSchema,
};

export const tool$customRolesPostCustomRole: ToolDefinition<typeof args> = {
  name: "custom-roles-post-custom-role",
  description: `Create custom role

Create a new custom role`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await customRolesPostCustomRole(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
