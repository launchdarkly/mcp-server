/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsBetaGetApplicationVersions } from "../../funcs/applicationsBetaGetApplicationVersions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApplicationVersionsRequest$inboundSchema,
};

export const tool$applicationsBetaGetApplicationVersions: ToolDefinition<
  typeof args
> = {
  name: "applications-(beta)-get-application-versions",
  description: `Get application versions by application key

Get a list of versions for a specific application in an account.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationsBetaGetApplicationVersions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
