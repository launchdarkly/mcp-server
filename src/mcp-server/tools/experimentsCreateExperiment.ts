/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsCreateExperiment } from "../../funcs/experimentsCreateExperiment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateExperimentRequest$inboundSchema,
};

export const tool$experimentsCreateExperiment: ToolDefinition<typeof args> = {
  name: "experiments-create-experiment",
  description: `Create experiment

Create an experiment.

To run this experiment, you'll need to [create an iteration](https://launchdarkly.com/docs/ld-docs/api/experiments/create-iteration) and then [update the experiment](https://launchdarkly.com/docs/ld-docs/api/experiments/patch-experiment) with the \`startIteration\` instruction.

To learn more, read [Creating experiments](https://launchdarkly.com/docs/home/experimentation/create).
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsCreateExperiment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
