/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { dataExportDestinationsGetDestination } from "../../funcs/dataExportDestinationsGetDestination.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetDestinationRequest$inboundSchema,
};

export const tool$dataExportDestinationsGetDestination: ToolDefinition<
  typeof args
> = {
  name: "data-export-destinations-get-destination",
  description: `Get destination

Get a single Data Export destination by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await dataExportDestinationsGetDestination(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
