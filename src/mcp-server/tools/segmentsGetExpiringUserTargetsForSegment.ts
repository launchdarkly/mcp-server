/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsGetExpiringUserTargetsForSegment } from "../../funcs/segmentsGetExpiringUserTargetsForSegment.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetExpiringUserTargetsForSegmentRequest$inboundSchema,
};

export const tool$segmentsGetExpiringUserTargetsForSegment: ToolDefinition<
  typeof args
> = {
  name: "segments-get-expiring-user-targets-for-segment",
  description: `Get expiring user targets for segment

> ### Contexts are now available
>
> After you have upgraded your LaunchDarkly SDK to use contexts instead of users, you should use [Get expiring targets for segment](https://launchdarkly.com/docs/ld-docs/api/segments/get-expiring-targets-for-segment) instead of this endpoint. To learn more, read [Contexts](https://launchdarkly.com/docs/home/observability/contexts).

Get a list of a segment's user targets that are scheduled for removal.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await segmentsGetExpiringUserTargetsForSegment(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
