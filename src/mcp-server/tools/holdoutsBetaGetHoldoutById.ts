/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { holdoutsBetaGetHoldoutById } from "../../funcs/holdoutsBetaGetHoldoutById.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetHoldoutByIdRequest$inboundSchema,
};

export const tool$holdoutsBetaGetHoldoutById: ToolDefinition<typeof args> = {
  name: "holdouts-(beta)-get-holdout-by-id",
  description: `Get Holdout by Id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await holdoutsBetaGetHoldoutById(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
