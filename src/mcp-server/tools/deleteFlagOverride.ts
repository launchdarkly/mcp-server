/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { deleteFlagOverride } from "../../funcs/deleteFlagOverride.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFlagOverrideRequest$inboundSchema,
};

export const tool$deleteFlagOverride: ToolDefinition<typeof args> = {
  name: "delete-feature-flag-value-override",
  description:
    `Deletes a local feature flag value override. This resets the feature flag to its original value.`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await deleteFlagOverride(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
