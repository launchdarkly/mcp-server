/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextsDeleteContextInstances } from "../../funcs/contextsDeleteContextInstances.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteContextInstancesRequest$inboundSchema,
};

export const tool$contextsDeleteContextInstances: ToolDefinition<typeof args> =
  {
    name: "contexts-delete-context-instances",
    description: `Delete context instances

Delete context instances by ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await contextsDeleteContextInstances(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
