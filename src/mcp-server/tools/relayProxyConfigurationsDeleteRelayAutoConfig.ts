/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { relayProxyConfigurationsDeleteRelayAutoConfig } from "../../funcs/relayProxyConfigurationsDeleteRelayAutoConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteRelayAutoConfigRequest$inboundSchema,
};

export const tool$relayProxyConfigurationsDeleteRelayAutoConfig: ToolDefinition<
  typeof args
> = {
  name: "relay-proxy-configurations-delete-relay-auto-config",
  description: `Delete Relay Proxy config by ID

Delete a Relay Proxy config.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await relayProxyConfigurationsDeleteRelayAutoConfig(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
