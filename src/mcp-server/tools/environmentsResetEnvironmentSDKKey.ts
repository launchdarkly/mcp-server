/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsResetEnvironmentSDKKey } from "../../funcs/environmentsResetEnvironmentSDKKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ResetEnvironmentSDKKeyRequest$inboundSchema,
};

export const tool$environmentsResetEnvironmentSDKKey: ToolDefinition<
  typeof args
> = {
  name: "environments-reset-environment-SDK-key",
  description: `Reset environment SDK key

Reset an environment's SDK key with an optional expiry time for the old key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await environmentsResetEnvironmentSDKKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
