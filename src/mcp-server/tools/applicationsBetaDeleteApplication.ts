/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { applicationsBetaDeleteApplication } from "../../funcs/applicationsBetaDeleteApplication.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApplicationRequest$inboundSchema,
};

export const tool$applicationsBetaDeleteApplication: ToolDefinition<
  typeof args
> = {
  name: "applications-(beta)-delete-application",
  description: `Delete application

Delete an application.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await applicationsBetaDeleteApplication(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
