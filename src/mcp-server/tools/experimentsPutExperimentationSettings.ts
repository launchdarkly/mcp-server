/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { experimentsPutExperimentationSettings } from "../../funcs/experimentsPutExperimentationSettings.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutExperimentationSettingsRequest$inboundSchema,
};

export const tool$experimentsPutExperimentationSettings: ToolDefinition<
  typeof args
> = {
  name: "experiments-put-experimentation-settings",
  description: `Update experimentation settings

Update experimentation settings for the given project`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await experimentsPutExperimentationSettings(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
