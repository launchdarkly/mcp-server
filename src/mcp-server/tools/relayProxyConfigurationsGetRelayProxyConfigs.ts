/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { relayProxyConfigurationsGetRelayProxyConfigs } from "../../funcs/relayProxyConfigurationsGetRelayProxyConfigs.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$relayProxyConfigurationsGetRelayProxyConfigs: ToolDefinition =
  {
    name: "relay-proxy-configurations-get-relay-proxy-configs",
    description: `List Relay Proxy configs

Get a list of Relay Proxy configurations in the account.`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await relayProxyConfigurationsGetRelayProxyConfigs(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
