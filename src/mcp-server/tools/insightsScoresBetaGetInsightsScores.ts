/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsScoresBetaGetInsightsScores } from "../../funcs/insightsScoresBetaGetInsightsScores.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetInsightsScoresRequest$inboundSchema,
};

export const tool$insightsScoresBetaGetInsightsScores: ToolDefinition<
  typeof args
> = {
  name: "insights-scores-(beta)-get-insights-scores",
  description: `Get insight scores

Return insights scores, based on the given parameters. This data is also used in engineering insights metrics views.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await insightsScoresBetaGetInsightsScores(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
