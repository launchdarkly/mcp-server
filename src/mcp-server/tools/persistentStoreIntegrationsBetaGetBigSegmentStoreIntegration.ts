/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { persistentStoreIntegrationsBetaGetBigSegmentStoreIntegration } from "../../funcs/persistentStoreIntegrationsBetaGetBigSegmentStoreIntegration.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetBigSegmentStoreIntegrationRequest$inboundSchema,
};

export const tool$persistentStoreIntegrationsBetaGetBigSegmentStoreIntegration:
  ToolDefinition<typeof args> = {
    name:
      "persistent-store-integrations-(beta)-get-big-segment-store-integration",
    description: `Get big segment store integration by ID

Get a big segment store integration by ID.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await persistentStoreIntegrationsBetaGetBigSegmentStoreIntegration(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
