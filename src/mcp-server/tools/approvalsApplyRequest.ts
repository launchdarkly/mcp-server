/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsApplyRequest } from "../../funcs/approvalsApplyRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApprovalRequestApplyForFlagRequest$inboundSchema,
};

export const tool$approvalsApplyRequest: ToolDefinition<typeof args> = {
  name: "apply-flag-approval-request",
  description:
    `Applies an approved approval request, executing the proposed changes to the feature flag. Can only be used after the request has received the required number of approvals. Completes the approval workflow by making the changes live.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsApplyRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
