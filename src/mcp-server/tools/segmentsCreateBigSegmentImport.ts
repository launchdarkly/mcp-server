/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { segmentsCreateBigSegmentImport } from "../../funcs/segmentsCreateBigSegmentImport.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateBigSegmentImportRequest$inboundSchema,
};

export const tool$segmentsCreateBigSegmentImport: ToolDefinition<typeof args> =
  {
    name: "segments-create-big-segment-import",
    description: `Create big segment import

Start a new import process for a big segment. This is an import for a list-based segment that can include more than 15,000 entries.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await segmentsCreateBigSegmentImport(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
