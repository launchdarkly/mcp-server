/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { announcementsGetAnnouncementsPublic } from "../../funcs/announcementsGetAnnouncementsPublic.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAnnouncementsPublicRequest$inboundSchema,
};

export const tool$announcementsGetAnnouncementsPublic: ToolDefinition<
  typeof args
> = {
  name: "announcements-get-announcements-public",
  description: `Get announcements

Get announcements`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await announcementsGetAnnouncementsPublic(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
