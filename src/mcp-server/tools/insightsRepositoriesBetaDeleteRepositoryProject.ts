/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { insightsRepositoriesBetaDeleteRepositoryProject } from "../../funcs/insightsRepositoriesBetaDeleteRepositoryProject.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteRepositoryProjectRequest$inboundSchema,
};

export const tool$insightsRepositoriesBetaDeleteRepositoryProject:
  ToolDefinition<typeof args> = {
    name: "insights-repositories-(beta)-delete-repository-project",
    description: `Remove repository project association

Remove repository project association`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await insightsRepositoriesBetaDeleteRepositoryProject(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
