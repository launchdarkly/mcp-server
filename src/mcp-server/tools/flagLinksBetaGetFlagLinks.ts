/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagLinksBetaGetFlagLinks } from "../../funcs/flagLinksBetaGetFlagLinks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFlagLinksRequest$inboundSchema,
};

export const tool$flagLinksBetaGetFlagLinks: ToolDefinition<typeof args> = {
  name: "flag-links-(beta)-get-flag-links",
  description: `List flag links

Get a list of all flag links.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagLinksBetaGetFlagLinks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
