/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { relayProxyConfigurationsResetRelayAutoConfig } from "../../funcs/relayProxyConfigurationsResetRelayAutoConfig.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ResetRelayAutoConfigRequest$inboundSchema,
};

export const tool$relayProxyConfigurationsResetRelayAutoConfig: ToolDefinition<
  typeof args
> = {
  name: "relay-proxy-configurations-reset-relay-auto-config",
  description: `Reset Relay Proxy configuration key

Reset a Relay Proxy configuration's secret key with an optional expiry time for the old key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await relayProxyConfigurationsResetRelayAutoConfig(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
