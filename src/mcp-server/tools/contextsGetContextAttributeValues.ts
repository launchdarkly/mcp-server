/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { contextsGetContextAttributeValues } from "../../funcs/contextsGetContextAttributeValues.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetContextAttributeValuesRequest$inboundSchema,
};

export const tool$contextsGetContextAttributeValues: ToolDefinition<
  typeof args
> = {
  name: "contexts-get-context-attribute-values",
  description: `Get context attribute values

Get context attribute values.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await contextsGetContextAttributeValues(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
