/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsPostApprovalRequestReviewForFlag } from "../../funcs/approvalsPostApprovalRequestReviewForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApprovalRequestReviewForFlagRequest$inboundSchema,
};

export const tool$approvalsPostApprovalRequestReviewForFlag: ToolDefinition<
  typeof args
> = {
  name: "approvals-post-approval-request-review-for-flag",
  description: `Review approval request for a flag

Review an approval request by approving or denying changes.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsPostApprovalRequestReviewForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
