/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { oAuth2ClientsCreateOAuth2Client } from "../../funcs/oAuth2ClientsCreateOAuth2Client.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.OauthClientPost$inboundSchema,
};

export const tool$oAuth2ClientsCreateOAuth2Client: ToolDefinition<typeof args> =
  {
    name: "O-auth2-clients-create-O-auth2-client",
    description: `Create a LaunchDarkly OAuth 2.0 client

Create (register) a LaunchDarkly OAuth2 client. OAuth2 clients allow you to build custom integrations using LaunchDarkly as your identity provider.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await oAuth2ClientsCreateOAuth2Client(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
