/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledChangesGetFeatureFlagScheduledChange } from "../../funcs/scheduledChangesGetFeatureFlagScheduledChange.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetFeatureFlagScheduledChangeRequest$inboundSchema,
};

export const tool$scheduledChangesGetFeatureFlagScheduledChange: ToolDefinition<
  typeof args
> = {
  name: "scheduled-changes-get-feature-flag-scheduled-change",
  description: `Get a scheduled change

Get a scheduled change that will be applied to the feature flag by ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await scheduledChangesGetFeatureFlagScheduledChange(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
