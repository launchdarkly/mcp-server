/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { layersCreateLayer } from "../../funcs/layersCreateLayer.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.CreateLayerRequest$inboundSchema,
};

export const tool$layersCreateLayer: ToolDefinition<typeof args> = {
  name: "layers-create-layer",
  description: `Create layer

Create a layer. Experiments running in the same layer are granted mutually-exclusive traffic.
`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await layersCreateLayer(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
