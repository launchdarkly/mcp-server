/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesPutBranch } from "../../funcs/codeReferencesPutBranch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PutBranchRequest$inboundSchema,
};

export const tool$codeReferencesPutBranch: ToolDefinition<typeof args> = {
  name: "code-references-put-branch",
  description: `Upsert branch

Create a new branch if it doesn't exist, or update the branch if it already exists.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesPutBranch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
