/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { releasePipelinesBetaGetReleasePipelineByKey } from "../../funcs/releasePipelinesBetaGetReleasePipelineByKey.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetReleasePipelineByKeyRequest$inboundSchema,
};

export const tool$releasePipelinesBetaGetReleasePipelineByKey: ToolDefinition<
  typeof args
> = {
  name: "release-pipelines-(beta)-get-release-pipeline-by-key",
  description: `Get release pipeline by key

Get a release pipeline by key`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await releasePipelinesBetaGetReleasePipelineByKey(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
