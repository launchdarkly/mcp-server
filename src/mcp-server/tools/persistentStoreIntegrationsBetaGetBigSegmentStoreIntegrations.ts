/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { persistentStoreIntegrationsBetaGetBigSegmentStoreIntegrations } from "../../funcs/persistentStoreIntegrationsBetaGetBigSegmentStoreIntegrations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$persistentStoreIntegrationsBetaGetBigSegmentStoreIntegrations:
  ToolDefinition = {
    name:
      "persistent-store-integrations-(beta)-get-big-segment-store-integrations",
    description: `List all big segment store integrations

List all big segment store integrations.`,
    tool: async (client, ctx) => {
      const [result, apiCall] =
        await persistentStoreIntegrationsBetaGetBigSegmentStoreIntegrations(
          client,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
