/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsDeleteFlagRequest } from "../../funcs/approvalsDeleteFlagRequest.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteApprovalRequestForFlagRequest$inboundSchema,
};

export const tool$approvalsDeleteFlagRequest: ToolDefinition<typeof args> = {
  name: "delete-flag-approval-request",
  description:
    `Deletes an approval request for a feature flag. Used by agents to withdraw a pending change request that is no longer needed or was created in error.
`,
  scopes: ["write"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsDeleteFlagRequest(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
