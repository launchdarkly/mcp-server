/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { codeReferencesPostExtinction } from "../../funcs/codeReferencesPostExtinction.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostExtinctionRequest$inboundSchema,
};

export const tool$codeReferencesPostExtinction: ToolDefinition<typeof args> = {
  name: "code-references-post-extinction",
  description: `Create extinction

Create a new extinction.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await codeReferencesPostExtinction(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
