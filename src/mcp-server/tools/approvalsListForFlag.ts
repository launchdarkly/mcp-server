/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsListForFlag } from "../../funcs/approvalsListForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetApprovalsForFlagRequest$inboundSchema,
};

export const tool$approvalsListForFlag: ToolDefinition<typeof args> = {
  name: "list-flag-approval-requests",
  description:
    `Lists all approval requests for a specific feature flag in a given environment. Useful for agents to check if there are pending changes awaiting review before attempting direct modifications.
`,
  scopes: ["read"],
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsListForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
