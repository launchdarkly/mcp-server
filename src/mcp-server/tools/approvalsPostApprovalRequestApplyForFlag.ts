/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { approvalsPostApprovalRequestApplyForFlag } from "../../funcs/approvalsPostApprovalRequestApplyForFlag.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostApprovalRequestApplyForFlagRequest$inboundSchema,
};

export const tool$approvalsPostApprovalRequestApplyForFlag: ToolDefinition<
  typeof args
> = {
  name: "approvals-post-approval-request-apply-for-flag",
  description: `Apply approval request for a flag

Apply an approval request that has been approved. This endpoint requires a feature flag key, and can only be used for applying approval requests on flags.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await approvalsPostApprovalRequestApplyForFlag(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
