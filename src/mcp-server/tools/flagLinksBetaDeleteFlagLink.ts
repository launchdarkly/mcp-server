/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { flagLinksBetaDeleteFlagLink } from "../../funcs/flagLinksBetaDeleteFlagLink.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFlagLinkRequest$inboundSchema,
};

export const tool$flagLinksBetaDeleteFlagLink: ToolDefinition<typeof args> = {
  name: "flag-links-(beta)-delete-flag-link",
  description: `Delete flag link

Delete a flag link by ID or key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await flagLinksBetaDeleteFlagLink(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
